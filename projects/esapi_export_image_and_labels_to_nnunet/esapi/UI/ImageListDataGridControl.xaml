<UserControl x:Class="esapi.UI.ImageListDataGridControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:esapi.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ContentControl.Resources>
        <Style x:Key="RowTextBlockStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5, 3"/>
        </Style>
    </ContentControl.Resources>
    <Grid>
        <DataGrid 
          ItemsSource="{Binding ImageList}"
          SelectedItem="{Binding SelectedImage}"
          AutoGenerateColumns="False"
          SelectionMode="Single"
          IsReadOnly="True"
          Margin="5"                 
          >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" ElementStyle="{StaticResource RowTextBlockStyle}"/>
                <DataGridTextColumn Header="Modality" Binding="{Binding Series.Modality}" ElementStyle="{StaticResource RowTextBlockStyle}"/>
                <DataGridTextColumn Header="Date" Binding="{Binding CreationDateTime}" ElementStyle="{StaticResource RowTextBlockStyle}"/>
                <DataGridTextColumn Header="Size" Binding="{Binding SizeString}" ElementStyle="{StaticResource RowTextBlockStyle}"/>
                <DataGridTextColumn Header="Resolution" Binding="{Binding ResolutionString}" ElementStyle="{StaticResource RowTextBlockStyle}"/>
                <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" ElementStyle="{StaticResource RowTextBlockStyle}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
